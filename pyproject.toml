[project]
name = "opsscribe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.4",
    "langchain-openai>=1.2.2",
    "pandas>=2.3.0",
]

[tool.uv]
package = false
